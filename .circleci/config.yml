version: 2.1

orbs:
  architect: giantswarm/architect@2.2.0

workflows:
  test:
    jobs:
      - architect/go-build:
          name: go-build
          binary: athena
          filters:
            tags:
              only: /^v.*/
